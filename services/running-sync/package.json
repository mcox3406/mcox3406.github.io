{
  "name": "personal-running-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "test:runtime": "npm run check && node --test tests/runtime.test.cjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "miniflare": "5.20260908.0-alpha", "wrangler": "4.130.0" }
}
